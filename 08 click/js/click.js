// Create the HTML for the message
var msg = '<div class=\"header\"><a id=\"close\" href="#">close X</a></div>';
msg += '<div><h2>System Maintenance</h2>';
msg += 'Our servers are being updated between 3 and 4 am';
msg += 'During this time, there may be minor distruptions to service.</div>';

// Create a new 'div' element

// Give the div's 'id' attribute of 'note'

// Set the HTML in between the opening and closing 'div' tags

// Append the element to the body of the page


function dismissNote() {
	// Remove the element from the body of the page
	
}

// Get the close button we created

// Close the note when the button is pressed
