// Declare a function
function checkUsername() {
	// Get the feedback HTML element
	
	// If username is too short
	if (this.value.length < 5) {
		// Set msg
		
	}
	else {
		// Otherwise clear message
		
	}
}

// Get the username input text box

// Add the event listener to the input text box
